osx_image: xcode9.2
language: objective-c
script:
- xcodebuild -sdk iphonesimulator -workspace RSSReader.xcworkspace -scheme AllTests-iOS -destination 'platform=iOS Simulator,name=iPhone X,OS=11.2' test CODE_SIGNING_REQUIRED=NO
before_install:
- brew install git-lfs
before_script:
- git lfs pull
